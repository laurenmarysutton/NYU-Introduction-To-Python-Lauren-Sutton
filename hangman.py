class Hangman():

    def __init__(self, word, guesses=0, guessed_letters=[]):
        self.word = word
        self.guesses = guesses
        self.guessed_letters = guessed_letters

    def guesses_less_than_5(self, word, guesses, guessed_letters)


    def set_intersection(self, word, guesses, guessed_letters):

if set(word).intersection(set(guessed_letters)) == set(word):
    print('You won!')
else:
    print('You lost!')

def main():
    magic_word = Hangman('dorito')
    magic_word = guesses_less_than_5(word=magic_word.word, guesses=magic_word.guesses)
    magic_word.set_intersection(word=magic_word.word, guesses=magic_word.guesses)

if __name__ = "__main__"
    main()
